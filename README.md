# `@leveluptuts/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @leveluptuts/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@leveluptuts/prettier-config"
}
```
